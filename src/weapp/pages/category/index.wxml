<van-search placeholder="Search categories" shape="round" disabled background="#ffffff" />

<!-- Category Tabs -->
<van-tabs
  sticky
  swipeable
  animated
  active="{{ active }}"
  bind:change="onTabChange"
>
  <van-tab
    wx:for="{{ categories }}"
    wx:key="id"
    title="{{ item.name }}"
    name="{{ item.id }}"
  >
    <van-cell-group inset>
      <van-cell title="{{ item.name }}" label="Browse {{ item.name }}" border="false" />
    </van-cell-group>

    <van-grid column-num="4" clickable>
      <van-grid-item
        wx:for="{{ item.children }}"
        wx:key="id"
        icon="{{ child.icon || 'apps-o' }}"
        text="{{ child.name }}"
      />
    </van-grid>
  </van-tab>
</van-tabs>
